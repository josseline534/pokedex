import React from "react";
import { SafeAreaView, Text } from "react-native";

const AccountScreen = () => {
    return (
        <SafeAreaView>
            <Text>Account</Text>
        </SafeAreaView>
    );
};

export default AccountScreen;
